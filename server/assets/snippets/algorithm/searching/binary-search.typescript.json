{
  "id": "binary-search.typescript",
  "slug": "binary-search",
  "title": "Binary Search",
  "language": "typescript",
  "category": "algorithm",
  "subcategory": "searching",
  "tags": ["searching", "divide-and-conquer", "array"],
  "lines": 15,
  "difficulty": "beginner",
  "code": "function binarySearch(arr: number[], target: number): number {\n  let left = 0;\n  let right = arr.length - 1;\n  while (left <= right) {\n    const mid = left + Math.floor((right - left) / 2);\n    if (arr[mid] === target) {\n      return mid;\n    } else if (arr[mid] < target) {\n      left = mid + 1;\n    } else {\n      right = mid - 1;\n    }\n  }\n  return -1;\n}",
  "description": "TypeScript binary search on a sorted array. Uses `Math.floor` for integer division and the overflow-safe midpoint formula. Runs in **O(log n)** time."
}
