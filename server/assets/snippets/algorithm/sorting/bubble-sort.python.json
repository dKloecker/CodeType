{
  "id": "bubble-sort.python",
  "slug": "bubble-sort",
  "title": "Bubble Sort",
  "language": "python",
  "category": "algorithm",
  "subcategory": "sorting",
  "tags": ["sorting", "comparison", "in-place"],
  "lines": 11,
  "difficulty": "beginner",
  "code": "def bubble_sort(arr: list[int]) -> list[int]:\n    n = len(arr)\n    for i in range(n):\n        swapped = False\n        for j in range(0, n - i - 1):\n            if arr[j] > arr[j + 1]:\n                arr[j], arr[j + 1] = arr[j + 1], arr[j]\n                swapped = True\n        if not swapped:\n            break\n    return arr",
  "description": "Repeatedly swaps adjacent elements that are out of order. The `swapped` flag enables early termination when the array is already sorted. **O(n^2)** worst case, but **O(n)** best case on nearly sorted input."
}
