{
  "id": "bubble-sort.typescript",
  "slug": "bubble-sort",
  "title": "Bubble Sort",
  "language": "typescript",
  "category": "algorithm",
  "subcategory": "sorting",
  "tags": ["sorting", "comparison", "in-place"],
  "lines": 14,
  "difficulty": "beginner",
  "code": "function bubbleSort(arr: number[]): number[] {\n  const n = arr.length;\n  for (let i = 0; i < n; i++) {\n    let swapped = false;\n    for (let j = 0; j < n - i - 1; j++) {\n      if (arr[j] > arr[j + 1]) {\n        [arr[j], arr[j + 1]] = [arr[j + 1], arr[j]];\n        swapped = true;\n      }\n    }\n    if (!swapped) break;\n  }\n  return arr;\n}",
  "description": "TypeScript bubble sort using destructuring assignment for swaps. The early-exit optimization with `swapped` makes it efficient on nearly sorted arrays. **O(n^2)** worst case."
}
