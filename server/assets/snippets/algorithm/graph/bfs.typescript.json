{
  "id": "bfs.typescript",
  "slug": "bfs",
  "title": "Breadth-First Search",
  "language": "typescript",
  "category": "algorithm",
  "subcategory": "graph",
  "tags": ["graph", "traversal", "queue"],
  "lines": 17,
  "difficulty": "intermediate",
  "code": "function bfs(graph: Map<number, number[]>, start: number): number[] {\n  const visited = new Set<number>();\n  const queue: number[] = [start];\n  visited.add(start);\n  const order: number[] = [];\n  while (queue.length > 0) {\n    const node = queue.shift()!;\n    order.push(node);\n    for (const neighbor of graph.get(node) ?? []) {\n      if (!visited.has(neighbor)) {\n        visited.add(neighbor);\n        queue.push(neighbor);\n      }\n    }\n  }\n  return order;\n}",
  "description": "TypeScript BFS using `Map` and `Set` for the adjacency list and visited tracking. Uses `queue.shift()` for FIFO behavior. Runs in **O(V + E)** time — ideal for level-order traversal and shortest-path in unweighted graphs."
}
